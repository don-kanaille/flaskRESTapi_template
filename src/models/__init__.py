"""
A model is our internal representation of an entity.

So, the model essentially is a helper
that allows our program or that gives us more flexibility
in our program without polluting the resource,
which is what the clients interact with.
"""
